import React from 'react';
// import './AppContainer.css'; // Create separate CSS if needed

const AppContainer = ({ children }) => {
  return <div className="app-container">{children}</div>;
};

export default AppContainer;